<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.DisasterApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary_color</item>
    </style>

    <style name="DisasterBottomSheetFragment" parent="Widget.Material3.BottomSheet.Modal">
        <!-- Apply attributes here -->
    </style>

    <style name="Button" parent="Theme.Material3.DayNight">
        <item name="android:backgroundTint">@color/primary_color</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Button.White" parent="ThemeOverlay.AppCompat">
        <item name="colorAccent">@android:color/white</item>
    </style>


    <style name="Theme.DisasterApp" parent="Base.Theme.DisasterApp" />

    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">
            @color/white
        </item>
        <item name="windowSplashScreenAnimatedIcon">
            @drawable/logo_app_disaster
        </item>
        <item name="postSplashScreenTheme">
            @style/Base.Theme.DisasterApp
        </item>
    </style>
</resources>